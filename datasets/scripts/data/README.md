# Data scripts

1. Install requirements:
   pip install -r requirements-datasets.txt
2. Run:
   bash scripts/data/full_build_pipeline.sh
3. For ASSISTments and EdNet, drop the official files into the corresponding folders after download.
